// Re-export client-side utilities
export { createClient, supabase } from './client';

// Note: Server-side utilities should be imported directly from './server'
// to ensure proper tree-shaking and avoid client-side bundle issues

